// Since we are using Next.js's built-in support for environment variables,
// we do not need to call config() from dotenv.
// The variables will be available via process.env.
// import { config } from 'dotenv';
// config();

import '@/ai/flows/skill-based-project-recommendations.ts';
